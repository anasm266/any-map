---
"any-map": minor
---

Preserve intermediate re-export hops in the flow graph, including named and `export *` chains, and improve explicit `: any` function return propagation into downstream callers.
