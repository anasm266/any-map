---
"any-map": minor
---

Milestone **m6**: `scan --format table|json|dot`, `any-map graph [-o file]`, `--source-kinds` / `--ignore` (picomatch), `--fail-above` / `--fail-coverage`, `runFullScan` + `serializedGraphToDot`, composite action `.github/actions/any-map-scan`.
