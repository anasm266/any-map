---
"any-map": minor
---

Milestone **m4**: forward propagation (`infectedBy` on graph nodes), blast-radius ranking, `sourcesRankedByBlast` on scan summary / `--json`, `any-map scan --top <n>`, table columns Rank + Blast.
